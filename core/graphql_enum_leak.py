def run(domain):
    print(f"[GraphQL Enum Leak] Running scan for {domain}...")
    # Example logic placeholder
    # You can use introspection queries or check /graphql endpoint behavior here
    print(f"[GraphQL Enum Leak] Scan complete for {domain}")
